# 📌 Phase 1 — Planning & Project Foundation

---

## 📍 1. Project Initialization & Repository Setup

- Create project requirement document
- Finalize tech stack (MERN + tools like JWT, Razorpay, Cloudinary, etc.)
- Initialize GitHub repository (private or public)
- Set up `main` branch + `develop` branch
- Add `.gitignore` file for Node.js & React
- Add `README.md` with project overview
- Plan folder structure for both frontend & backend
- Decide project naming conventions (components, folders, APIs, models)
- Add issue templates / commit message format (optional but good practice)

---

## 📍 2. Database Schema Design

- Identify all entities (users, products, orders, cart, categories, etc.)
- Define relationships between collections
- List all fields for each collection
- Decide validation rules and constraints
- Identify indexes for fast search performance
- Decide image storage method (DB URL or cloud storage)
- Design ER (Entity Relationship) diagram
- Document each DB model clearly

---

## 📍 3. API Endpoint Planning

- Identify all required modules (Auth, Products, Cart, Orders, Payments)
- List APIs needed per module
- Define request & response payload for each API
- Add authentication & authorization rules per API
- Plan middlewares (JWT auth, admin verify, error handler)
- Decide pagination / filtering / sorting strategy for products API
- Document full API route list in a table format

---

## 📍 4. UI/UX Wireframing

- List all pages/screens required (Home, Product Page, Cart, Login, Checkout, etc.)
- Sketch rough layout of each page
- Define navigation flow between pages
- Choose color palette, typography, UI style guide
- Decide component hierarchy (header, banners, product cards, footer, etc.)
- Design mobile-first UI considerations
- Finalize reusable components (Button, Card, Input, Modal, etc.)
- Create final wireframes (Figma / Canva / Pen & Paper — any is fine)

---
